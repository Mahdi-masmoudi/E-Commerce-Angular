import { AuthconfigInterceptor } from './authconfig.interceptor';

describe('AuthconfigInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthconfigInterceptor()).toBeTruthy();
  });
});
